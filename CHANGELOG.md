# Changelog

All notable changes to botpanel are documented here.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

---

## [Unreleased]

### Docs
- docs: add mandatory Agent usage policy to CLAUDE.md; correct `withAuth` signature, Redis event mechanism (replace phantom `events/bus.ts` with real `publishInvalidate`), deployment section (one-tag/one-clone, rollback note), Stack (custom UI kit, no shadcn/radix), Auth model (V2 flags for team-list and SUDO_ROLE_IDS), panel-owned schema notes, local dev commands, env vars table, and CHANGELOG style note.

### Changed — performance & reliability pass
- **Capability resolution now runs in one parallel round and is memoized per request.** `resolveAccess()`'s three backing lookups (`sudo_users` check, `auto_channels` voice ownership, otter `business.user_ranks` RPC) previously ran sudo-first-then-the-rest; they now run in a single `Promise.all`, the `sudo_users` query is skipped entirely when `SUDO_USER_IDS` already grants sudo, and the per-user capability load is wrapped in React `cache()` so the `(dashboard)` layout + page (which each resolve access in the same render pass by design) share one set of Postgres/RPC round-trips per navigation instead of two. Route handlers are unaffected (React's cache scope is absent there — same behaviour as before). `SUDO_USER_IDS` is also parsed once at module load instead of per call.
- **`getSession()` memoized per request** via React `cache()` — layout + page no longer verify the same session JWT twice per navigation. Route handlers (the only places that mutate the session cookie) run uncached, exactly as before.
- **New `withAuth` option `resolveCaps: false`** (only honored with `require: 'any'`): hands the handler a session-only AccessMap (identity from the JWT, empty capabilities — strictly less privilege, can never widen access) and skips the Postgres + bot-RPC capability lookups. Applied to the three hot endpoints whose handlers never read capabilities: `GET /api/health/bots` (polled every 30s per tab — the health pills now also keep working while the bot databases are down, which is exactly when you want them), `GET /api/csrf`, and `GET /api/push/config`.
- **`/me` page loads in one parallel round** — the viewed-user display-name RPC and the three Squishy reads (voice-channel names, active voice membership, game-pref counts) were four sequential awaits; now a single `Promise.all` with each loader keeping its own best-effort fallback.
- **/otter/businesses index**: the owner-count and role-mapping-count aggregates now run in parallel (`Promise.allSettled`, preserving the per-query degrade behaviour) instead of back-to-back.
- **`resolveUsernames` chunk fan-out**: >100-id lookups (cold-cache members/audit pages) now fire their per-100 `users.resolve` RPC chunks concurrently instead of serially, so worst-case bot timeouts overlap instead of stacking.
- **Otter-ranks cache no longer grows unbounded**: expired per-user entries in the 60s rank cache are now swept opportunistically on insert (previously one stale entry lingered per distinct visitor for the process lifetime).

### Fixed
- **View-As Exit / Sign out / OAuth-state now actually clear their cookies (critical).** All three cookies use the `__Host-` prefix, whose spec requires every `Set-Cookie` (including the one that clears it) to carry `Secure` + `Path=/`. `cookies().delete(name)` emits a bare expiry without those attributes, so the browser **silently rejected the deletion** — Exit View-As returned 200 but left `__Host-view-as` in place (operator stuck impersonating, only a manual cookie-clear escaped), and `clearSessionCookie()` left `__Host-session` in place (logout didn't truly sign you out). Replaced `c.delete()` with an explicit overwrite-with-expiry (`maxAge: 0` + the same `secure`/`sameSite`/`path` attributes) in `clearViewAsCookie` (`viewAs.ts`), `clearSessionCookie` (`session.ts`), and the OAuth-state cleanup (`auth/callback`). Pre-existing bug surfaced now that the refreshed shell makes View-As prominent.

### Added — UI overhaul, phase 1 (foundation)
- **Discord-style multi-column app shell.** Replaced the flat link-list sidebar (`Sidebar.tsx`, removed) + thin `DashboardShell` with a new layout: a left **icon rail** (`LeftRail.tsx`) that switches between top-level sections (Overview / Squishy / Otter / Sudo, each with its own accent + active "pill" indicator), a contextual **section sidebar** (`SectionSidebar.tsx`) showing only the active section's groups, and a **top bar** (`TopBar.tsx`) with breadcrumb, search, live bot health, and user menu. Driven by a single serializable nav model (`(dashboard)/nav.ts`, `buildNav`) that ports the old capability + guild-membership gating verbatim, consumed by the rail, sidebar, and command palette so the three never drift. Interactive layout/state lives in a new client `AppFrame.tsx`; `DashboardShell.tsx` is now a thin server transform (build nav, snapshot health, reduce AccessMap → serializable props).
- **⌘K / Ctrl-K command palette** (`CommandPalette.tsx`): fuzzy search across every nav item the viewer can see (respects the same gating as the sidebar) plus stateful actions (Exit View-As, Sign out). Keyboard-first (↑/↓/Enter/Esc), portaled above all stacking contexts.
- **Live bot-health pills in the top bar** (`BotHealth.tsx`) seeded from a server snapshot and polling the new `GET /api/health/bots` every 30s. Backed by `getShellHealth()` added to `lib/heartbeats.ts` (normalizes the heartbeat map → `{ online, version, uptimeSec, lastBeatSec, guildCount }`; offline/stale bots are absent and rendered offline client-side).
- **Global toast host** (`ToastHost.tsx`) — finally mounts the long-existing `useToast()` module bus once in the shell, so any client write can surface feedback without per-page plumbing.
- **Reusable UI kit** under `web/src/components/ui/` (barrel `index.ts`): `Button`/`buttonClasses`, `Card` family + `Eyebrow`, `Badge`, `Input`/`Textarea`/`Select`/`Label`/`Field`/`Hint`, `Switch`, `Dialog`, `Tabs`, `Skeleton`/`EmptyState`/`StatCard`/`PageHeader`, a `cn` classnames helper, a self-contained inline-SVG `Icon` set (~50 glyphs) + `Spinner`. No new runtime deps — icons are bespoke so the build never depends on an external icon package.
- **User menu** (`UserMenu.tsx`) — avatar dropdown in the top bar (dashboard / edit profile / report / sign out), View-As aware.

### Changed
- **New color system + design tokens** (`tailwind.config.ts`): kept every pre-existing token NAME (`bg`/`bg-card`/`bg-card2`, `line`, `ink`/`ink-dim`, `accent`, `ok`/`warn`/`err`) so all 33 existing pages adopt the refresh with zero per-page edits — only the values changed (blue-violet near-black surfaces, an iris/violet brand `accent`, cyan/gold/rose section accents). Added elevation surfaces (`bg-app`/`bg-rail`/`bg-sidebar`/`bg-raised`/`bg-hover`), `line-bright`, `ink-faint`, an `accent` ramp (`bright`/`soft`), semantic `info`, depth shadows (`card`/`pop`/`glow`), and entrance animations (`fade-in`/`scale-in`/`slide-up`).
- **`globals.css`**: thin custom scrollbars matching the dark surfaces, brand selection color, a `.no-scrollbar` utility for the rail.
- **`ViewAsBanner`** now renders as a flush full-width strip at the top of the new shell (dropped the old `md:pl-52` sidebar offset).
- Sidebar version badge / "What's new" dialog now lives in the section sidebar footer (`ChangelogButton` reused unchanged). Bumped `web/package.json` to `0.4.0` and added the user-facing 0.4.0 changelog entry.

> Phase 1 is the foundation: every page immediately gets the new chrome, palette, and primitives. Subsequent phases restyle individual pages onto the kit and expand features (live dashboards, message composer everywhere, deep member/profile admin, full Otter business admin) area-by-area.

---

## [0.3.0] — 2026-06-06

### Added
- **Sidebar version badge + "What's new" changelog dialog.** The sidebar footer (directly below **Sign out**) now shows the current app version; clicking it opens a modal of curated, user-facing release notes. The version string + notes live in `web/src/lib/changelog.ts` (`APP_VERSION` is derived from the newest entry, so the badge tracks the top of the list), and the dialog lives in `web/src/app/(dashboard)/ChangelogButton.tsx` — portaled to `document.body` so the sidebar's `z`-stacking context doesn't trap it, with Escape / backdrop-click to close and background-scroll lock while open. This is the user-facing companion to this file; cutting a release means adding an entry in both.
- **Reusable Components-V2 message editor (`web/src/components/msgeditor/` + `web/src/lib/msgspec/`).** A portable, feature-agnostic embed designer: structured container/text/section-with-accessory/separator/image-gallery/link-button builder with reorder, an accent-color picker (swatches + native picker), a Discord-markdown toolbar (bold/italic/underline/strike/code/code-block/heading/subtext/quote/bullet/link), a dynamic-variable insert menu, a full Discord timestamp builder (`<t:UNIX:R>` and every style), and a live Discord-styled WYSIWYG preview. The message format is a versioned zod-validated "message spec" (`msgspec/schema.ts`) any panel feature can reuse.
- **Game Night scheduler at `/squishy/game-night`.** Design a post in the editor with `{{game}}`/`{{when:F}}`/`{{host}}`/`{{rsvp}}`/etc. variables, target a channel, set the event time, then **Post now** or **Schedule** for a specific time. Posts get auto RSVP / own-a-copy buttons whose state is persisted in the DB (survives bot restarts). List shows scheduled/posted/failed/canceled with Send-now / Edit / Delete. New nav link under **Squishy → Game Night**.
- **API routes** `POST /api/squishy/scheduled-posts`, `PATCH|DELETE /api/squishy/scheduled-posts/[id]`, `POST /api/squishy/scheduled-posts/[id]/send` — sudo-gated, CSRF + rate-limited, audited; spec validated server-side via `parseMessageSpec`. Send-now / live-post retract go through the new `scheduled_post.send` / `scheduled_post.cancel` bot verbs.
- **Vendored `scheduled_posts` schema** (`web/src/lib/db/schema/squishy/scheduledPosts.ts`) synced from squishybot.
- **Steam link for game-night posts.** A "Steam link" URL field exposes a `{{steam}}` variable you can drop anywhere (any text line or a button URL), and the starter template ships a "🎮 Open in Steam" link button that auto-hides (in preview and on render) when no URL is set.

---

## [0.1.1] — 2026-05-24

### Fixed
- **Security (#237): `PUT /api/squishy/settings/[key]` now enforces per-key numeric bounds via a panel-side `NUMERIC_SETTINGS` registry.** The generic settings PUT only validated key shape + non-empty string + ≤4096 chars — a sudo could persist garbage like `play.default_cooldown_seconds = "hello"` or `voice.cleanup_delay_ms = "-1"` from the documented operator UX. squishybot#130 added defense-in-depth on the bot's sudo modal but the panel (the higher-volume write surface) had no equivalent. New `src/lib/squishy/settings-registry.ts` mirrors the bot's `NUMERIC_SETTINGS` const (only `key` + `min` + `max` — labels/descriptions stay bot-side) and exposes `validateNumericSetting(key, value)` which the route calls after the existing string checks. Non-numeric keys (e.g. `welcome.message_template`) fall through unaffected. Drift between the two lists is a SECURITY bug and the registry comment + `// #237` route comment point at the bot source for grep-ability. Rejections audit with `success: false` like the other validation paths.

_v0.1.1 · 6f07060_

---

## [0.1.2] — 2026-06-05

### Docs
- **Full README rewrite + wiki overhaul to match the shipped panel.** The README still described the repo as "Phase 0 — landing page only" with Next.js + auth "coming in MVP," but the panel is fully built: Discord OAuth, capability-based auth (`resolveAccess` → flat map, not tiers), `withAuth` gating + CSRF + in-memory rate limit, View-As, the HMAC Redis command bus (`callBot`/`botrpc.ts`), heartbeat aggregation (`heartbeats.ts`/`redis.ts`), SSE streams, cache-invalidation publisher (`events/invalidate.ts`), OAuth refresh-token encryption at rest (`OAUTH_TOKEN_KEY`/`tokenCrypto.ts`), and Web Push (VAPID). Rewrote to the standard structure (Overview → Architecture → Stack → Quick start → Configuration → Usage → Deployment → Conventions) with a complete env table, a panel-area table, the real Redis integration, and the correct loopback-only/no-public-ports + standalone-cloudflared topology. Removed all references to the deleted `dev` branch / dev clone. Overhauled the seven wiki pages (Architecture, Auth-Model, Bot-Integration, Deployment, Home, Roadmap, Security-Checklist) to the same current reality — notably fixing Bot-Integration's nonexistent `events/bus.ts`/`events/types.ts` references and the stale RPC envelope shape, and flipping the Roadmap from "MVP/V2/V3 planned" to shipped.
- **Added a `description` to `web/package.json`** ("Web control panel for the Squishy & Otter Discord bots").

_v0.1.2 · 1ebf09d_

---

## [0.2.1] — 2026-06-05

### Ops
- **Watchtower image pinned to the maintained `nickfedor/watchtower:latest` fork.** The original `containrrr/watchtower` is unmaintained and its newest images speak a Docker API version this VPS's older Docker daemon can't satisfy, so it would fail to poll/pull on the host. `docker-compose.yml` already points the `watchtower` service at `nickfedor/watchtower:latest` (the actively-maintained drop-in fork with current Docker API support); this entry back-fills that switch into the changelog and confirms it as the canonical image for the auto-pull-and-restart sidecar. No behavior change to the `com.centurylinklabs.watchtower.enable="true"` label contract — only the image source moved.

_v0.2.1 · 3aaf806_

---

## [0.2.0] — 2026-06-05

### Fixed
- **Web Push: VAPID public key now served from a runtime API route instead of `NEXT_PUBLIC_VAPID_PUBLIC`.** The Next.js `NEXT_PUBLIC_*` convention inlines values at build time, so the CI image baked without VAPID_PUBLIC set had `undefined` inlined into the client bundle — the operator couldn't fix it by editing `.env` + restarting. Replaced the build-time inline with a new `GET /api/push/config` route that returns `{ publicKey: env.VAPID_PUBLIC }`; the `<PushOptIn>` component fetches on mount. Operators now need exactly three vars (`VAPID_PUBLIC`, `VAPID_PRIVATE`, `VAPID_SUBJECT`) and a container restart is enough to rotate keys. Closes #218.

### Added
- **`/otter/businesses/[slug]`: custom command buttons editor.** Manager+ (or bot owner) of a discord-only business can add, edit, reorder, enable/disable and delete the custom **Link** / **Info** buttons that show up on the business's slash command (`/oc`, `/caked`, `/info`). New `<BusinessButtonsEditor>` island + `loadBusinessButtons()` loader, driven by new API routes `POST|PUT /api/otter/businesses/[slug]/buttons` and `PATCH|DELETE /api/otter/businesses/[slug]/buttons/[id]` which forward to the bot's `business_buttons.*` RPC verbs (the bot owns the table + cache and re-verifies manager+ rank). Every write is CSRF-checked, rate-limited and audited. No `otterDb` access — reads + writes go over RPC, so no vendored schema is required (the `business_buttons` table is auto-vendored once otterbot merges). Pairs with the otterbot custom-buttons PR.
- **`/squishy/games`: "📣 Post LFG" button per row.** Triggers the same Components V2 LFG panel the slash command produces, with optional message + ping-the-role toggle. Inline collapsible form per row — textarea (max 500 chars, mirrors the slash `message` option), checkbox for ping (mirrors `ping`). Submit POSTs `/api/squishy/play` which calls the new squishybot `play.post` RPC verb; bot owns the actual posting + four-button panel (Help / Notify Toggle / I-want-to-play / Cancel). Host attribution: `access.viewing.id`, so View-As works. Audit row records `actor` + `viewing` + game + `{ hasMessage, ping, channelId, messageId }` in `after`. Same 30-min/(user,game) cooldown the slash command has; panel can't bypass it. Closes #216. Pairs with squishybot `play.post` verb in the matching squishybot PR.
- **`/me` dashboard: "Currently in voice" + "Game preferences" cards.** Two new sections on the user dashboard so the daily-driver actions (control the voice channel I'm sitting in, manage which games ping me) are one click away from login. The voice card only renders when the viewing user is actually in an auto-voice channel — looked up by joining `auto_channel_members` ↔ `auto_channels` for `user_id = access.viewing.id`, taking the most-recently-joined row — and the button label flips between "Controls →" (if `access.squishy.voiceChannels` includes the row, meaning owner / acting-owner / host / sudo / bot-owner) and "View →" otherwise. The game-prefs card counts `user_game_prefs` rows split by `wants_view` / `wants_ping` with an empty state for users who haven't opted into anything; both stat lines link to the existing `/me/games` editor. Both queries are best-effort: a DB hiccup falls through to a silent skip (voice card) or "unavailable" hint (prefs card) instead of faulting the page. View-As-aware — when sudo is impersonating someone, the cards reflect the viewed user's voice/prefs. Closes #210.
- **Web Push notifications for new staff approvals + reports (V3-5).** Sudo + bot-owner can opt in per-browser via the new `<PushOptIn>` card on `/sudo` (top of page, all sudo) and `/me` (bot-owner only). The new `POST /api/push/subscribe` upserts into a panel-owned `push_subscriptions` table (lazy `CREATE TABLE IF NOT EXISTS` on the squishy Postgres; migration also lives at `web/src/lib/db/migrations/0001_push_subscriptions.sql`), and `DELETE /api/push/subscribe` drops it. New `src/lib/push/dispatch.ts → notifyAll(title, body, url)` fans out via `webpush.sendNotification` to every row, auto-prunes endpoints that return HTTP 404/410, and is wired into `POST /api/squishy/staff/request`, `POST /api/squishy/report`, and `POST /api/otter/report` as fire-and-forget `void notifyAll(...)` so a push outage never fails the underlying write. Service worker at `web/public/sw.js` renders the notification and routes click-throughs to the right page (defaults to `/sudo`). VAPID keys are operator-supplied (`VAPID_PUBLIC` / `VAPID_PRIVATE` / `VAPID_SUBJECT`) — without them the subscribe routes return 503 and the dispatcher logs+no-ops, so a deploy without push configured still works. iOS Safari needs Add-to-Home-Screen / PWA mode to receive push (Apple constraint); desktop browsers + macOS Safari work natively. Closes #205.
- **Panel-side cache-invalidate publisher — `publishInvalidate(bot, {table, key?})`.** First piece of V3-1: writes that mutate a bot-cached row now publish an HMAC-signed event on `bot.<botname>.settings.invalidate` so the bot can reload its in-memory cache without restart. Wired into `PUT/DELETE /api/squishy/settings/[key]` (covers every `bot_settings` key: welcome/goodbye, staff role mappings, channel IDs, social-feed config, etc). Fire-and-forget — a redis hiccup or missing `BOTPANEL_RPC_SECRET` never fails the underlying write. Other write surfaces (`games`, `hub_channels`, `auto_thread_channels`, `social_feeds`, otter business tables) will adopt the helper in follow-up PRs. Closes #33 once bot subscribers ship in matching PRs on squishybot + otterbot.
- **View-As UI exposure.** `resolveAccess(session, { viewAsUserId })` was already wired server-side and every audit row already records both `actor` + `viewing` — what was missing was a way for sudo / bot-owner to actually flip the bit without manually editing a cookie. New `__Host-view-as` cookie (12h, see `src/lib/auth/viewAs.ts`) is read by `withAuth` and by the `(dashboard)` layout so every API + page sees the impersonated capabilities. New routes `POST /api/sudo/view-as` (set cookie, emits `auth.view_as_started`) and `DELETE /api/sudo/view-as` (clear cookie, emits `auth.view_as_ended`); self-View-As returns 400 `{ error: 'self' }`. New "View As" card on `/sudo` with a `<MemberPicker bot='squishy'>` + Start / Exit buttons. New sticky red banner across every authed page ("Viewing as @… — your changes audit as your real account → @… Exit") that DELETEs the cookie and bounces back to `/sudo`. Sidebar swaps to the viewed user's avatar + name with an `(as)` tag while View-As is on; `/me` likewise shows the viewed user's identity card so the actor can sanity-check what the impersonated user sees. Closes #204.

### Ops
- **`dev` branch + dev clone removed entirely. Workflow is now main-only.** All future work targets `main` directly via PRs; no long-lived intermediate branch. `.github/workflows/deploy.yml` no longer triggers on `dev`. `CLAUDE.md` rule 6 rewritten. Both prior dev-related Ops entries below (subdomain migration, basePath removal) are superseded — left in the log for historical accuracy. Issue #191 (subdomain migration) closed as moot. Tracking: #196.
- **(superseded by removal above)** Dev clone briefly served at `https://dev-bots.tucker.host/` (its own subdomain) instead of `bots.tucker.host/dev/` (path prefix). Cloudflared ingress, `NEXT_PUBLIC_BASE_PATH=/dev` removal, Caddyfile / landing/script.js comment cleanup, CLAUDE.md URL updates all shipped — then the entire dev environment was removed.
- **TypeScript 6.0 compat: add `web/globals.d.ts` with `declare module '*.css';`** TS 6.0 tightened side-effect imports of non-TS files and stopped honoring Next's bundled CSS module declaration; without this, `import './globals.css'` in `src/app/layout.tsx` fails type-check during `next build`. Paired with the typescript 5.9.3 → 6.0.3 bump.
- **Auto-PR vendored bot schemas on `repository_dispatch`.** New `.github/workflows/sync-bot-schema.yml` listens for `bot-schema-changed` events from otterbot and squishybot. Re-runs `scripts/sync-schema.sh`, opens (or updates) one rolling PR on `auto/sync-bot-schemas` if there's drift. Closes the race where merging a bot schema change put `main` into a red `verify-schemas` state for several minutes (incident: 2026-05-12 `business_messages`). Manual fire via Actions UI for backfilling / testing. Auth: `BOTPANEL_DISPATCH_PAT`.

### Changed
- **`/me` voice-channel list shows names + renders each row as a tappable button.** Was a wrap of tiny font-mono snowflakes that didn't tell you which channel was which. The page now resolves each id against `auto_channels` server-side (manual name → fallback name → bare id), and renders each as a full-width row with the name prominent, the id as a muted mono subtitle, and a hover affordance + chevron. Raw-id rendering still happens if the DB lookup misses (channel just disappeared, or `squishyDb` unreachable). Click still goes to `/squishy/voice`.

### Ops (cont'd)
- **CI now runs on pull requests (not just `push` to main/dev).** `deploy.yml` gained a `pull_request:` trigger; PRs run `verify-schemas` + a full Docker build of both images, but skip the GHCR push (`push: ${{ github.event_name == 'push' }}`). FLOATING_TAG resolves to `pr-<number>` on PRs to keep the tag string valid even though it's unused. `NEXT_PUBLIC_BASE_PATH` resolves from `github.base_ref` on PRs so a PR targeting `dev` builds with `/dev` like the dev clone does. Closes the gap where merging to dev was the only way to know if the build worked.

### Security
- **AEAD-encrypt OAuth refresh tokens at rest + admin "logout-all" endpoint.** New `src/lib/auth/tokenCrypto.ts` (AES-256-GCM, `node:crypto`, no new deps) encrypts each user's Discord refresh token under a new `OAUTH_TOKEN_KEY` env (32 bytes hex; generate with `openssl rand -hex 32`). Encrypted columns land on a new panel-owned `panel_sessions` table (DDL in `web/src/lib/db/migrations/0001_panel_sessions_aead.sql` — apply manually since botpanel doesn't run drizzle-kit) with `refresh_token_ciphertext` / `_iv` / `_tag` / `_key_version` so we can rotate the key without breaking decrypt. The OAuth callback now mints a server-side jti, encrypts the refresh token, and persists the row (best-effort — login still works if the DB is unreachable). New `POST /api/admin/auth/logout-all` (bot-owner, CSRF-checked, 1/hour rate-limited) truncates every `panel_sessions` row except the actor's and audits as `auth.logout_all`. Plaintext refresh-token column kept nullable for the follow-up backfill PR; that PR also drops it. Closes #203.
- **Force `postcss` ≥ 8.5.10 via `pnpm.overrides` to close CVE-2026-41305 (GHSA-qx2v-qp2m-jg93, moderate, CVSS 6.1).** Next.js 15.5.18 hard-pins `postcss@8.4.31` as a transitive, which is below the patched version. Tailwind/autoprefixer were already on 8.5.14 so two postcss copies coexisted in the lockfile; the Next-pinned one was the vulnerable one. The CVE is an XSS via unescaped `</style>` in postcss's stringifier — only exploitable when user-supplied CSS is parsed and re-stringified into a runtime `<style>` tag, which this app never does (postcss runs at Tailwind build time only). Fixed for lockfile hygiene; no runtime risk existed. Override scoped to `postcss@<8.5.10` so future Next bumps that move past the pin auto-drop the override.

### Fixed
- **`/otter/businesses/[slug]` — "Manage by ID (advanced)" now takes a snowflake, not a member search.** The block's whole purpose is to act on users who aren't on the live roster (just-left, ban-grant targets), but it was wired to `<MemberPicker bot="otter">` — a typeahead over the bot's cached members. Off-roster users were unfindable, and pasting a raw snowflake didn't help because the picker only sets its hidden `userId` field when you click a dropdown row. Net effect: every submit landed at the API with `userId=""`, which Zod-rejected as `invalid`. Swapped the picker for a plain `<input pattern="\d{15,25}">` so paste-and-submit works.

### Performance
- **Discord avatars now load via `next/image` with the `cdn.discordapp.com` domain allowlisted in `next.config.mjs`.** Avatars get auto-WebP conversion, lazy loading below the fold, and srcset for high-DPI displays. Most impactful on the audit table, members browser, and voice page where 25+ rows render at once. Swapped raw `<img>` for `<Image>` in `UserChip`, `MemberPicker`, `MembersBrowser`, `VoiceLive`, `VoiceControls` (MemberInline), `Sidebar`, `/me` page, and Otter `EmployeePanel`. The user's own avatar in `Sidebar` and `/me` is marked `priority` (above the fold on every authed view).
- **`/squishy/members/[id]` audit username resolution now runs in the same parallel batch as the 8-way data load.** The `resolveUsernames('squishy', auditUserIds)` call was a separate `await` after the Promise.all — on a cold cache that serialized a 5-20s `users.resolve` RPC behind every other section's DB query. Chained via `.then()` inside the Promise.all so the rest of the page loads in parallel with the bot round-trip.
- **SSE leak in `<VoiceLive>` and `<AuditLive>` closed.** Both components now close the prior EventSource on effect-entry (in addition to the cleanup return) via a shared `esRef`. Dev-mode strict-mode double-invocation and `router.refresh()` could schedule a fresh effect before the previous cleanup ran, leaving zombie connections that kept firing onmessage into stale closures.
- **`/me/games` catalog + user-prefs queries run in parallel and filter in SQL.** Were sequential awaits with a `SELECT *` then a JS-side `!isArchived && isVisible` filter. Now a single `Promise.all` with the visible/non-archived check pushed into the WHERE clause — fewer rows over the wire, no head-of-line blocking.

### Changed
- **Voice host promotion/demotion now reflects on the panel instantly.** `/squishy/voice` was ignoring the `voice.hosts_changed` SSE event. The page now handles it locally: mutates `hostUserIds` and recomputes `canControl` for the viewer using their session-side `viewerId` + `viewerIsPriv` flags (threaded from the server-rendered page). A user who just got promoted to host sees the Controls button appear without refreshing; a just-demoted host sees it disappear.

- **`/` auto-redirects signed-in viewers to `/me` when both bots are online.** The landing page is mostly a "is this thing on?" indicator for unauth'd or partial-outage states; if the viewer has a session AND every bot reports online AND there's no sign-in-error to surface, redirect straight to the dashboard.

### Added
- **Games editor: inline "+ Create" buttons and an "Auto-provision" checkbox.** `/squishy/games` now lets sudo fix a missing role/channel link without leaving the page — when a row's `roleId`, `pingRoleId`, or `channelId` is null, the inline editor shows a "+ Create channel" / "+ Create view role" / "+ Create ping role" button next to the picker. Clicking prompts for the name (default `🎮-{slug}` for channels, `{game.name}` / `{game.name} LFG` for roles), hits a new `/api/squishy/discord/create-channel` or `/api/squishy/discord/create-role` route (which calls the new squishybot `discord.create_channel` / `discord.create_role` RPC verbs), then PATCHes the games row to wire the freshly-created id in. New channels land in the bot's configured games category (`bot_settings.channel.games_category`). Top-of-page Add Game form gains an **"Auto-provision channel + view role + ping role"** checkbox — when checked, the form posts to a new `/api/squishy/games/provision` route that fires the squishybot `game.provision` RPC verb, which atomically creates a `🎮-{slug}` text channel at position 3 in the games category, a view role named after the game, a ping role named `{game} LFG`, and a games row wired to all three IDs. Best-effort rollback on partial failure. Audited as `discord.role_created` / `discord.channel_created` / `game.provisioned`.
- **Host management on `/squishy/voice` per-channel controls.** The "Hosts" section was a read-only placeholder ("host management lands in a later wave"); it's now a real editor: each existing host gets an `✕` remove button, and a `<MemberPicker>` + "Add host" button at the bottom appends a new one. Calls a new `/api/squishy/voice/[id]/hosts/toggle` route which fires the new squishybot `voice.toggle_host` RPC verb. Same gate as transfer/delete (owner / acting-owner / sudo / bot-owner) — hosts can't add their peers; that stays owner-only.

### Changed
- **Voice controls render `[avatar] @displayName` chips everywhere.** `/squishy/voice` per-channel popover passes the resolved-user map down to `<HostsList>` + `<DisconnectList>`. Owner line, host rows, and member-disconnect rows now show proper Discord chips instead of raw snowflakes. Confirm dialog + aria-labels use the friendly name.
- **Voice "Controls" button animates in place.** Was: clicking replaced the button with a static expanded card. Now: the same button morphs to "✕ Close" and the panel slides open below via a CSS grid-rows transition (200ms). All buttons get `transition-colors duration-150` on hover.
- **OC stock status cards drop 🟢🟠🔴 emoji** in favor of an outline + colored chip (`In stock` / `Low stock` / `Out of stock`). Same color cue, no emoji glyph. Card body gains a subtle hover background transition.
- **Bot status cards on `/` drop 🟢/🔴 emoji** for a small colored dot (emerald / red) and a matching ring on the card.
- **OC "Post to channel" channel input is now a `<ChannelPicker bot="otter">`** dropdown of text + announcement channels. Falls back to a snowflake input if the otter bus is unreachable. The picker calls a new `/api/otter/meta/channels` route which proxies to the new `meta.list_channels` verb on otterbot.

### Added
- **`<ChannelPicker bot="squishy" | "otter">` prop.** Was hardcoded to `/api/squishy/meta/channels`; now consumers can target `bot="otter"` (used by the OC post form). New `/api/otter/meta/channels` route + new `meta.list_channels` verb on otterbot (also `meta.list_roles` and `meta.list_members` since they all live in the same handler file).
- **`/squishy/members` sudo Members editor.** New sudo-only browser at `/squishy/members` lists every guild member with avatar + display name + raw snowflake, debounced 200ms typeahead search (reuses `meta.list_members` from the existing `<MemberPicker>` plumbing), and a "Load more" button that pages 25 at a time up to the bot's 100-cap. Clicking a row drills into `/squishy/members/[id]` — a one-screen editor where sudo edits every per-user setting Squishy tracks: Profile (reuses the existing `<ProfileEditor mode="sudo">`), Games (mirrors `/me/games` parameterized on target), Sudo toggle (insert/delete `sudo_users` + fires `admin.reload_caches` so the bot's in-memory cache catches up), Staff Roles (the lifted DirectGrant/DirectRevoke forms with the target pinned), Voice presence (lists every auto-channel the user owns/hosts/joined with inline Force-disconnect + Transfer-to-this-user buttons), Color Role (only when `feature.color_roles` is on — pulls curated rows from `color_roles` table + a Clear button), Pending Staff Requests for this user (with inline Approve/Deny), and the last 50 audit rows where the user is the actor OR target. New API routes `/api/squishy/members/[id]/games`, `/sudo`, `/color-role`, all gated `require: 'sudo'` + CSRF + 30/min rate limit. Page does its own `resolveAccess(session)` gate + a second `resolveAccess(session, {viewAsUserId: id})` so downstream components see the target's capability map. Audited as `member.games_set` / `member.sudo_toggled` / `member.color_set`. Sidebar gets a new "Members" link in the Squishy group (near Profiles). Also lifts `<ApproveButton>` / `<DenyButton>` / `<DirectGrantForm>` / `<DirectRevokeForm>` out of `/sudo/StaffApprovalControls.tsx` into a shared `@/components/staff/DirectStaffControls` module so both `/sudo` and the new members editor consume the same source — DirectGrant/Revoke gained optional `defaultUserId` props (renders the user-id field as a hidden input when set) and every callback got an `onSuccess` opt-out so callers can suppress the default `router.refresh()` if they're already wired to a different refetch path.
- **`/me/games` self-service page.** Any logged-in member can pick which games they want View (channel access) and LFG-ping roles for. Lists the full visible+non-archived catalog from `games` against the viewer's `user_game_prefs` rows; a single Save batches every toggle into one round-trip via the new `games.set_prefs` RPC verb on squishybot. Bot applies the role grants exactly like the in-server `/games` slash flow (same `setPref` helper, same view-required-for-ping rule, same cascade-off-when-view-disabled side effect). Sidebar gets a new top-level "My game prefs" link. Tight rate-limit (10/min/actor) on the API route. Audited as `games.prefs_set`.
- **`/report` self-service page.** Any logged-in member can file a bug / feature request / question against either squishybot or otterbot — two forms, one per bot, each mirroring its bot's `/report` slash modal (Title 5–200, Type select bug/feature/question, Description 10–2000, optional Steps 1000). Posts go through new `report.submit` RPC verbs on each bot which DM the owner with the same Approve/Reject buttons the slash flow uses. Tight rate-limit (3 per 10 min/actor) matches the bot's 5-min slash cooldown. Sidebar gets a new top-level "Report a bug" link. Audited as `report.submitted` with `{title, type}` only — description goes to the owner DM, not the audit row.
- **`/squishy/voice` is now visible to all logged-in users, filtered per-viewer.** Was sudo-only; now any logged-in user sees the channels they have a real relationship with (currently in / own / host / acting-owner). Sudo / bot-owner still see everything. The per-channel "Controls" popover stays gated on owner/host/acting-owner/sudo (matches Discord — you can't rename someone else's room). The SSE event stream applies the same filter live: a member joining a channel extends their visibility on the next event; leaving contracts it. Sidebar gets a top-level "Active Voice" link visible to everyone (above the sudo-gated Squishy group).

- **Edit `/caked` and `/oc` card text from the panel.** Caked managers/owners can now edit the Contact / Event / Pricing card bodies that `/caked` shows; OC managers/owners can edit the Requirements card from `/oc`. Both pages got a new "Edit message content" section above the existing post-to-channel form. Saves go through new `business_messages.update` / `.reset` RPC verbs which enforce manager+ rank server-side and audit every change.

### Fixed

- **`/me/games` save no longer surfaces an opaque "fetch fails" / bare-`timeout` banner.** Bumped the `games.set_prefs` RPC timeout from the 5s default to 20s so the bot has time to do its uncached `members.fetch()` + per-row channel-overwrite + ping-role grant cycle; mapped each known transport-level error (`timeout`, `redis-down`, `rpc-not-configured`) onto a user-readable sentence in the API response so the `<ServerForm>` error banner explains what to do (timeout tells the user to refresh — some/all rows may have applied). Also added a `useEffect` in `MyGamesEditor` so the `router.refresh()` post-save actually re-syncs the table state from the new server-rendered `rows` prop (previously `useState`'s initializer only ran on mount, leaving the table stuck on pre-save values until a hard reload).

- **Dashboard sidebar narrowed `w-60` → `w-48` (240px → 192px).** Main-column padding moved from `md:pl-60` to `md:pl-48` in `DashboardShell.tsx` and the matching `loading.tsx` skeleton. The narrowest label ("Welcome / Goodbye") still fits at the new width.

- **`/me/games` editor got a second green Save button at the top of the form** above the table — same submit as the bottom button, both inside the existing `<ServerForm>` so `fieldset[disabled]` covers them together during submit. Visually distinct (emerald CTA styling) so users editing a long catalog don't have to scroll back down to confirm.

### Changed

- **Sidebar now hides bot-specific nav for users who aren't in the relevant bot's guild.** OAuth callback fetches `https://discord.com/api/users/@me/guilds` alongside `/users/@me` and stores the IDs in the JWT (new optional `guildIds?: string[]` on `Session`). Sidebar compares `session.guildIds` against `env.GUILD_ID` for Squishy (single-guild) and uses the otter-business-rank map as a proxy for Otter (multi-guild). Squishy top-level links (Active Voice, My game prefs) AND the Squishy nav group are gated on `inSquishyGuild || botOwner`; Otter nav group on `inOtterGuild || botOwner`. `/report` page now renders each bot's form conditionally on the same flags. Legacy JWTs (no `guildIds` field) fall back to the prior capability-flag visibility so existing sessions don't suddenly lose access — new logins get the gating.

- **Otter perms resolution moved off broken SQL to `business.user_ranks` RPC.** `loadOtterBusinesses` was running `where m.user_id = $1` against `business_role_mappings` — but that table has no `user_id` column (it maps role-id-to-rank; user membership lives as Discord roles). Every panel page render was spamming `column m.user_id does not exist` and `column o.user_id does not exist` (also wrong on `business_owners`, which uses `discord_user_id`). Replaced both with a single call to the new otterbot `business.user_ranks({userId})` verb which walks the guild member cache + role mappings + owner table and returns `{slug: rank}`. 60s in-process cache per userId so common page loads don't round-trip the bot. Empty result on bot-down doesn't cache, so the user recovers when the bot comes back up.

- **`squishy.user_profiles` missing `UNIQUE(guild_id, user_id)` index added.** The Drizzle schema declared it but no migration ever applied it, so every profile PATCH was failing with `there is no unique or exclusion constraint matching the ON CONFLICT specification`. Created the index in prod (one duplicate row had to be deduped first — two empty placeholder rows for the same `(guild_id, user_id)` pair from a race in initial profile creation). Bot's schema is unchanged; just brought the actual DB in line with what was already declared in code.

- **Compose `redis` service now gated behind `profiles: ["primary"]`** to stop the dev clone from spinning up a second `redis` container with the same network alias. Two `redis` containers on the same docker network were round-robin'ing DNS — panel publishes from the dev clone were landing on dev-redis while the bot was psubscribe'd to prod-redis, and every `callBot()` timed out (manifested as "Granted X · Y + ITSRI Staff, but 3 grant(s) failed: ... (timeout)" in the panel UI, plus echo-test timeouts on /sudo/rpc-test). Bring the prod clone up with `COMPOSE_PROFILES=primary docker compose up -d`; dev clone runs without that env and joins the shared prod redis on the botpanel-net network.

- **Staff base-role rename to match Discord: `it_cri_staff` → `itsri_staff`, "IT CRI Staff" → "ITSRI Staff".** Pairs with the matching squishybot commit. Updates the hardcoded role key in `/api/squishy/staff/request`'s sudo short-circuit path and every UI string in `StaffRequestCard.tsx` / `squishyStaffRoles.ts`. Safe rename — Provision & Link hadn't been run yet, so no `bot_settings` rows with the old key exist.

### Changed

- **Snowflake-userId text inputs across 5 surfaces (sudo add-user, sudo direct staff grant/revoke, voice transfer-ownership, otter EmployeePanel hire+manage, business owner add) are now `<MemberPicker>` typeahead dropdowns.** Same form contracts (POST `name` fields unchanged); each picker degrades to a snowflake fallback input with an amber warning banner if the bot is unreachable. Pairs with PR 1 (`/squishy/members` editor) — same MemberPicker, now universal. MemberPicker gained an optional `bot: 'squishy' | 'otter'` prop (default `'squishy'`) so otter consumers hit the new `/api/otter/meta/members` route (thin pass-through to OtterBot's `meta.list_members` RPC, same shape as the squishy route).

- **`/otter/businesses/[slug]` Members panel is now a real roster.** Was: a list of DB owners only + a free-form "Manage by user ID" input. Now: the bot's `business.roster` verb returns every member with any of this business's role mappings, grouped by rank, with per-row Promote / Demote / Fire buttons that hit the existing Wave 7c-B routes. MKE is excluded — its `/otter/mke` link-out page is the canonical surface for MKE staff. Skeleton while loading, 30s panel-side cache, owner-only guard for the "last owner" edge case so the panel never offers a way to delete the final owner.

- **Sudo path on `/api/squishy/staff/request` short-circuits to instant grant.** When the requester is sudo (or bot owner) AND submits the staff request card on their own profile, the panel bypasses the approval queue entirely: it calls `staff.grant` for the picked department, the picked tier, and the ITSRI Staff base role in turn (each grant is idempotent on the bot side). Audited as `staff.self_granted` with the full grant outcome blob (one entry per role key with `{ok, error?}`) so an unlinked-role failure is recoverable from the audit trail. The UI is updated to reflect this — the card header shows "Grant yourself a staff role" instead of "Request a staff role", a green "Sudo · instant" pill replaces the blue "Self" pill, and the submit button says "Grant immediately". On success the alert shows the full grant outcome including any per-role failures (with the hint to run `/sudo → Settings → Staff Roles → Provision & link`). Non-sudo users continue to go through the queued `staff.request` flow unchanged.

- **Removed the 4 freeform staff text inputs from the profile editor (`staffCategory`, `department`, `tier`, `leadershipTitle`).** They previously rendered as a "Staff fields (sudo only)" section but overlapped with the new dept/tier picker and were rarely used as intended. The underlying `user_profiles.staffCategory|department|tier|leadershipTitle` columns are unchanged — their existing values are preserved (the PATCH route guards every column with `!== undefined`, so submissions without those keys don't touch them). Sudo can still edit them via the API or DB if needed; the UI just no longer surfaces them. The page's "Staff fields are sudo-only" hint also goes away since there are no longer per-mode staff fields to hide.

### Changed
- **Staff request card on `/me/edit` reworked to dept + tier picker.** The card now has TWO dropdowns (Department / Tier) instead of one role select. Both optional individually; at least one required. The free-text "Why you want this role?" field is GONE — just dept, tier, optional real name. Pairs with squishybot's matching flow rewrite: approving grants the picked dept role + the picked tier role + the new **ITSRI Staff** base role automatically. `/api/squishy/staff/request` body shape changed accordingly: `{departmentSlug?, tierSlug?, realName?}`. Legacy pending requests (single-role shape) still render in the "you already have N pending" banner correctly — they get classified into department-or-tier on the fly.
- **`web/src/lib/squishyStaffRoles.ts`** split into `DEPARTMENT_OPTIONS` (5) and `TIER_OPTIONS` (3). Old generic `STAFF_ROLE_OPTIONS` removed. The ITSRI Staff base role isn't in either list because the requester never picks it explicitly — it's auto-granted on approval bot-side.

### Added
- **Self-service "Request a staff role" card on `/squishy/profiles/[id]/edit`.** When you're editing your OWN profile (the surface `/me/edit` redirects to), a new card under the profile form lets you pick a department + tier (both optional, at least one) and submit. Same flow as the `/settings → Staff Role` button on the bot. POSTs to `/api/squishy/staff/request` → `callBot('squishy', 'staff.request', ...)` → bot inserts the `staff_approvals` row AND posts the Components V2 approval card to `STAFF_APPROVAL_THREAD_ID`. Pending requests are loaded server-side and rendered above the form. Route is `withAuth({require:'any', csrf:true, rateLimit:{points:5, perSeconds:300}})` and audits as `staff.requested`.

### Fixed
- **TS narrowing in 4 employee write routes.** The `!reply.ok` audit-write branches in `/api/otter/businesses/[slug]/employees/{hire,fire,promote,demote}/route.ts` referenced `reply.data?.before` / `reply.data?.after`, but on the failure variant `BotRpcResult` has no `data` property. Replaced with `before: null, after: null` (the values are meaningless when the bot call failed). Success-branch refs unchanged — `callBot<{before, after}>` provides the right type there.
- **Merge-conflict hot fix in `/otter/businesses/[slug]/audit/page.tsx`.** The sequential merge of Wave 7d-C (#109, shared `<AuditTable>` refactor) and Wave 7d-B (#114, `<UserChip>` integration) on the same page left both render trees concatenated, breaking JSX. Resolved by dropping the ad-hoc `<ul>`/`<nav>` block — the new `<AuditTable>` already handles row chips internally per the 7d-C design. Removed now-unused `UserChip` / `relTime` / `resolveUsernames` imports and the `userMap` pre-resolution from this page (the in-row `resolved` Map still pre-fills display names from `audit_logs.actor_name`).

### Added
- **`<UserChip>` + `users.resolve` everywhere.** Audit tables, staff approvals, voice rosters now show `@displayName` with a small avatar instead of raw snowflakes. Batch-resolved server-side per page render (5-min in-process cache). Single-id endpoints `/api/{squishy,otter}/users/[id]` for live-updating client surfaces. Falls back to raw id if the bot is unreachable.
- **Discord resource pickers across all Squishy settings forms.** New `<RolePicker>`, `<ChannelPicker>`, `<MemberPicker>` client components fetch live cache from squishybot via three new `meta.*` RPC verbs (60s panel-side cache for roles/channels; live for member typeahead). All snowflake-text-inputs in Hubs / Games / Reaction Roles / Welcome editors replaced. Falls back to a snowflake input + error banner if the bot is unreachable, so forms still work in degraded mode.
- **`/otter/caked` Caked messaging page.** Manager-only post-to-channel page for contact / event / pricing / announcement cards. Routes through the bot's `caked.message_post` verb (renderers shared with `/caked` slash command). Audited as `caked.posted`.
- **`/otter/businesses/[slug]` gains hire/fire/promote/demote.** Four new write routes under `/api/otter/businesses/[slug]/employees/*` go through the matching bot verbs. Owner-only for hiring an owner, manager-or-owner for the rest. Each action audited with `before`/`after` rank.
- **`/otter/oc-stock` Post-to-channel.** New button posts the live stock card to a Discord channel via the bot's `oc.stock_post` verb. Available to anyone with an OC role mapping. Audited as `oc.stock_posted` with the resulting `{messageId, channelId}`.
- **`/otter/businesses/[slug]` gains a Sync Roles button (owner-only).** POSTs to `/api/otter/businesses/[slug]/sync-roles`, which calls the new bot-side `business.sync_roles` verb. Audited as `business.sync_roles` with the `{added, removed, skipped}` payload.

### Fixed
- **`web/src/app/(dashboard)/squishy/roles/RolesWriteUI.tsx` TS narrowing on `setError`.** The previous ternary collapsed to `{}` instead of `string | null` on the falsy branch — the `&&` chain's intermediate truthy `(parsed as object)` propagated. Split out `parsedErr` with a `typeof === 'string'` guard so `setError(msg)` gets a guaranteed string.

### Added
- **Shared `<AuditTable>` component for both squishy `setting_changes` and otter `audit_logs`.** Filter chips (action / actor / success), pagination, before/after diff in a collapsible `<details>`. Both `/squishy/audit` and `/otter/businesses/[slug]/audit` now share the same UI. Drops bot-specific audit-rendering code in both pages.
- **`/otter/mke` McKenzie staff page.** Read-only roster + role-mapping view for MKE staff with two prominent link-out cards (Lookup client / Lookup employee → https://mke.euphoric.gg/employee/portal/clients). Per the plan, MKE lookups are NEVER in-dashboard. Edit surface continues to live at `/otter/businesses/mke`.
- **Wave 7b — `/squishy/welcome` welcome + goodbye template editor with live preview.** New sudo-only page at `/squishy/welcome` with a two-column layout (Welcome | Goodbye). Each column edits three `bot_settings` rows (`welcome.enabled` / `welcome.channel_id` / `welcome.template` and the `goodbye.*` siblings) via the existing `PUT /api/squishy/settings/[key]` route from Wave 6 — no new write endpoint, no duplicate audit surface, each save audits one row exactly as today. Toggle is a checkbox that writes `"true"` / `"false"` (matches `getBoolSetting`'s parse contract); channel ID is a snowflake-validated text input (proper channel picker arrives in Wave 7d); body is a 2000-char textarea with the supported tokens (`{user}` `{server}` `{member_count}` `{account_age}`) called out in inline help. Each column has a **Preview** button that POSTs to the new `POST /api/squishy/welcome/preview` route (sudo, CSRF, 10/min/actor rate-limit) which calls `callBot('squishy', 'welcome.preview', { userId: access.viewing.id, kind })` — the bot renders the live template (same tokens, same defaults as the real join/leave handlers) and returns the string. **Preview is read-only on the bot side: it never posts to the configured channel** — the live `guildMemberAdd` / `guildMemberRemove` handlers still own the real welcome/goodbye posts, so calling preview cannot cause a double-post. Sidebar Squishy group gains "Welcome / Goodbye" between "Settings" and "Hubs". Page renders an inline preview card per column with status pills (`rendered` / `timeout` / `rpc-not-configured` / `bad-reply`) and a hint about `usingDefault: true` when no override is saved.

- **`/squishy/games` full CRUD with bot cache-refresh hook (Wave 7b).** The read-only games overview is now editable for sudo + bot owner. Top-of-page "Add game" form POSTs `name` (required, 1-100 chars) plus optional `channelId` / view `roleId` / `pingRoleId` (Discord snowflakes, 15-25 digits) and `playCooldownSeconds` / `autoArchiveDays` (non-negative ints) to `POST /api/squishy/games`, which INSERTs into `games` with `guildId = env.GUILD_ID` (NOT NULL — refuses with `GUILD_ID-unset` if missing) and audits `game.added`. Each row gains a collapsible inline `<EditGameForm>` that `PATCH /api/squishy/games/[id]`'s only the supplied fields (partial update; empty-string / null on a snowflake / number column explicitly clears it) and audits `game.updated`. Each row gains a flip-to-confirm `<RemoveGameButton>` (first click arms with red "Confirm remove" + Cancel + auto-revert after 4s, second click submits) that `DELETE /api/squishy/games/[id]`'s the row AND cascade-deletes the matching `user_game_prefs` rows in a single Drizzle `transaction(async tx => …)` — the FK already has `ON DELETE CASCADE` but doing it explicitly gives us a row-count for the audit blob and one atomic point of failure — and audits `game.removed`. All three routes are `withAuth({require:'sudo', csrf:true, rateLimit:{points:30, perSeconds:60}})` and audit success AND failure. After every successful write, the route fires `void callBot('squishy', 'games.refresh_cache', {})` so the bot reloads its in-memory `catalog` map — but the RPC reply is logged-not-blocking, and a bot-side timeout / `ok:false` never fails the panel write (the bot reads the games table live for /play and /games regardless; an out-of-sync cache heals on the next bot restart or sudo Debug → Force-clear caches). New client island at `web/src/app/(dashboard)/squishy/games/GamesWriteUI.tsx`; the server page re-runs its DB read via `router.refresh()` on each `onSuccess`.
- **`/sudo` admin home gains approve/deny + direct grant/revoke for staff roles (Wave 7b).** The "Pending staff approvals" card now ships an **Approve** (green) and **Deny** (gray, confirm) button on every row, plus a sudo-only **Direct grant** and **Direct revoke** form at the top of the card for the "I already decided offline" path. Four new API routes under `web/src/app/api/sudo/`:
  - `POST /staff-approvals/[id]/approve` — sets `staff_approvals.status='granted'` + `reviewed_by`/`reviewed_at` on the real actor, then `callBot('squishy', 'staff.grant', {userId, roleKey})`. The row is moved out of the queue **before** the Discord call so a downstream failure still leaves the queue in a defined state; the bot's reply rides in the response so the panel can surface "queue cleared but Discord didn't apply the role" as a one-shot alert. Audits `staff.approved` with `before`/`after.grant`.
  - `POST /staff-approvals/[id]/deny` — sets status to `denied`. Audits `staff.denied`.
  - `POST /staff/grant` — direct grant body `{userId, roleKey}`. Snowflake-validates the user id; role key must match the hand-rolled staff-role slug list (mirror of `squishybot/src/services/staffRoles.ts`). Audits `staff.direct_grant`.
  - `POST /staff/revoke` — direct revoke, same envelope. Audits `staff.direct_revoke`.
  All four routes are `withAuth({require:'sudo', csrf:true, rateLimit:{points:30, perSeconds:60}})` — sudo (not bot-owner-only) because the day-to-day approve/deny workflow shouldn't require owner authority. New `<ApproveButton>` / `<DenyButton>` / `<DirectGrantForm>` / `<DirectRevokeForm>` client components in `app/(dashboard)/sudo/StaffApprovalControls.tsx` ride `<ServerForm>` for CSRF + disabled-while-submitting + inline 4xx banners; on success they `router.refresh()` so the table re-fetches without a stale-data flash.
- **`/sudo/debug` — live admin ops via the command bus (Wave 7b).** The V2 footer note on the Debug Snapshot page is gone; three real buttons now live in a new top-of-page `<AdminOpsCard>` (client component). Each button is a `<ServerForm>` posting to a new bot-owner-only route under `/api/sudo/admin/`: `reload-caches` → `callBot('squishy', 'admin.reload_caches', {})` → green inline strip "Reloaded: settings, games, social_feeds, bot_owner"; `orphan-scan` → `callBot('squishy', 'admin.orphan_scan', {})` → table below the button with `{table, id, reason}` columns, or "No orphans found ✓" on empty; `reconciler` → `callBot('squishy', 'admin.reconciler_run', {})` with a `confirm:` dialog → green strip carrying `{recovered, cleaned, hubs, panels, adopted}` stats. All three routes wrap `withAuth({require:'botOwner', csrf:true, rateLimit:{points:5, perSeconds:60}})` (tight limit — these are heavy ops) and audit `admin.caches_reloaded` / `admin.orphan_scan_run` / `admin.reconciler_run` with the reply payload captured in `after` (orphan scan stores only the count, not the full list, to keep the audit row bounded). Any non-`ok:true` reply (timeout, rpc-not-configured, bot threw, …) surfaces in a red strip inline carrying the error string — never silently succeeds. The 403 card text on the page also lost its "MVP" language since the eventual write surface is now the actual write surface.
- **Reaction-role builder + per-message delete on `/squishy/roles?tab=reaction` (Wave 7b).** The previously read-only reaction tab gains two sudo-gated write surfaces, both going through the Wave-7a command bus rather than touching the DB directly — creating / destroying a reaction-role message means posting / deleting a real Discord message, and the rows are downstream of that. New `<CreateReactionRoleForm>` (collapsible "New message" card) and `<DeleteReactionRoleButton>` (flip-to-confirm) in `RolesWriteUI.tsx`. The builder takes a channel-ID text input (full channel picker ships in Wave 7d), a body textarea (≤2000 chars), a dynamic mappings list (1..20 rows of `{emoji, roleId}` with `+`/`✕` add/remove), and a "Temporary" checkbox that gates an "Expires in N minutes" input (1..43200 = 30 days). Bespoke fetch flow rather than `<ServerForm>` because the payload carries a `mappings[]` array — `ServerForm`'s FormData → flat-object collapse can't faithfully represent it. New JSON routes: `POST /api/squishy/reaction-roles` (sudo, CSRF, 10/min — tight because each call posts a Discord message) → `callBot('squishy', 'rxnroles.create', {channelId, body, mappings, isTemporary, expiresInMinutes})` with a 15 s timeout to absorb the message-post + up-to-20 reaction-seeds; and `POST /api/squishy/reaction-roles/[id]/delete` (sudo, CSRF, 10/min) — the path `[id]` is the row UUID, we resolve the Discord `messageId` via Drizzle and hand THAT to the bot (`callBot('squishy', 'rxnroles.delete', {messageId})`) because the bot's cache is keyed on `messageId`, not the panel's PK. Both routes audit success + failure via `writeAudit({action: 'rxnroles.created'|'rxnroles.deleted', ...})`. On RPC failure the bot's error code (`bad-channel`, `send-failed`, `not-found`, etc.) propagates verbatim into the form's red banner. On success the create form router.push's back to `/squishy/roles?tab=reaction` so the new message renders.
- **`/squishy/hubs` full CRUD + per-hub and all-hubs lockdown.** First Wave 7b consumer of the command bus. Page flips from pure read-only to editable when `access.squishy.sudo || access.botOwner`; everything is server-rendered, all writes go through `<ServerForm>` (`_format=json`, CSRF auto-injected) with `onSuccess={() => router.refresh()}` to re-fetch the table without a full nav.
  - **Guild-wide lockdown bar** at the very top — "Lock all hubs" (minutes input, default 60, cap 30 days) + "Unlock all hubs". Both `window.confirm`-gated. Status line flips to "Lockdown is currently active" when `bot_settings.voice.guild_lockdown_until` is in the future.
  - **"Add hub" form** — `voiceChannelId` (required, snowflake-validated) + optional `label` (≤100 chars; column default `'➕ Create Voice'` kicks in when blank), `position` (integer, default 0), and `categoryId` override. When `categoryId` is omitted we fall back to `bot_settings.channel.auto_voice_category`; we refuse with `categoryId-unset` (400) if both are missing because the column is NOT NULL.
  - **Per-row Lock / Unlock toggle** — renders Lock + a duration input (default 60 min) when the hub isn't currently locked, or Unlock when it is. Talks to `POST /api/squishy/hubs/[id]/lockdown` which looks up the channelId from the row UUID and calls `hub.lockdown` over the command bus. Always returns 200 with `{reply}`; the UI introspects nothing — it just refreshes.
  - **Per-row Edit** — collapsible inline form (label + position) hitting `PATCH /api/squishy/hubs/[id]`.
  - **Per-row Remove** — flip-to-confirm Remove → Confirm? / Yes / Cancel, hitting `DELETE /api/squishy/hubs/[id]`.
  - New routes under `web/src/app/api/squishy/hubs/`: `POST /` (insert + `hub.refresh_cache`), `PATCH /[id]` (partial update + refresh), `DELETE /[id]` (delete + refresh), `POST /[id]/lockdown` (`hub.lockdown` RPC), `POST /lockdown-all` (`hub.lockdown_all` RPC). All routes are `withAuth({require:'sudo', csrf:true, rateLimit:{points:30, perSeconds:60}})` except `lockdown-all` which uses `{points:10}` (high blast radius). Every route audits success AND every validation/RPC failure via `writeAudit({bot:'squishy', action:'hub.added'|'hub.updated'|'hub.removed'|'hub.lockdown', ...})` — `targetType:'all'` distinguishes guild-wide lockdown rows from per-hub flips in the unified audit tail.
  - Cache-refresh contract: DB-only writes (POST/PATCH/DELETE) fire-and-forget `hub.refresh_cache` (3s timeout) so the bot's `isHubChannelCached` hot path picks up the row immediately; the DB row is the source of truth, so an RPC failure logs a warning but doesn't fail the HTTP response.
- **Voice Control on `/squishy/voice` — Wave 7b.** The Active Voice Channels page gains a per-card "Controls" popover that drives the six voice-control verbs added on the squishybot side (see the matching `feat/wave7b-voice` branch over there). New API routes under `web/src/app/api/squishy/voice/[id]/` all wrap `withAuth({require:'any', csrf:true, rateLimit:{points:30, perSeconds:60}})`, re-load the `auto_channels` row, check `canControlChannel` (owner / host / acting-owner / sudo / bot-owner) before forwarding to the bot via `callBot('squishy', 'voice.*', ...)`, and audit every success AND rejection with `writeAudit({bot:'squishy', action:'voice.<verb>', actor, viewing, before, after, success, errorMessage?})`:
  - `POST /rename` body `{newName}` — capped at 100 chars.
  - `POST /lock` body `{locked}` — accepts boolean or `"true"`/`"false"` string for form-encoded callers.
  - `POST /hide` body `{hidden}`.
  - `POST /transfer` body `{newOwnerUserId}` — snowflake-validated, owner-or-sudo only (stricter gate via `canDestructivelyControlChannel`), `<ServerForm confirm="…">` prompt on the panel.
  - `DELETE /[id]` — owner-or-sudo only, `<ServerForm confirm="…">` prompt, destructive button styling.
  - `POST /disconnect` body `{userId}` — snowflake-validated, per-member ✕ button in the controls popover with a confirm prompt.
  `/api/squishy/voice/list` now returns `hostUserIds` + a server-computed `canControl: boolean` per row so the client never decides its own gating; the API routes also re-gate so the worst a snoop can do is poke the URLs directly and get a 403. New `VoiceControls.tsx` renders the popover (rename input, lock/hide toggle row, read-only hosts list, transfer-by-ID input, per-member disconnect ✕, destructive Delete) — each action is a self-contained `<ServerForm>` so CSRF + rate-limit + error banner come for free. On a successful submit, the parent `<VoiceLive>` re-fetches the snapshot (rename has no bot event today, and `canControl` is recomputed on every snapshot) — the SSE feed still picks up lock/hide/owner/delete on its own. New shared helper `app/api/squishy/voice/_lib.ts` exports `loadAutoChannel`, `canControlChannel`, `canDestructivelyControlChannel`, `isSnowflake`. Host management is intentionally out of scope for Wave 7b — surfaces as a read-only list with a "use Discord for now" note.
- **Panel-side Redis command-bus client — Wave 7a foundation.** New `web/src/lib/botrpc.ts` exposes `callBot<T>(bot, verb, params, opts?)` for issuing `cmd.<bot>.<verb>` requests with an HMAC-SHA256 envelope (`{requestId, ts, hmac, params}`) signed with `BOTPANEL_RPC_SECRET` and awaiting the reply on `res.<requestId>`. The client uses a separate lazy-singleton ioredis publisher (the existing `lib/redis.ts` is in subscriber mode and can't `PUBLISH`) plus a fresh per-call subscriber that SUBSCRIBEs to the reply channel **before** publishing the request — avoids the race where the bot's reply arrives before we're listening. Default timeout 5s; on timeout or HMAC mismatch (bot drops silently) the call returns `{ok:false, error:'timeout'}`. Other failure modes surface as `rpc-not-configured` (env unset), `redis-down`, `bad-reply`. Companion `web/src/lib/util/hmac.ts` exposes `hmacSha256` + `timingSafeCompare`. New `POST /api/admin/rpc-test` (bot-owner-only, CSRF + 10/min rate limit) accepts `{bot, message}` and calls `callBot(bot, 'echo', {message})` for smoke testing. New `/sudo/rpc-test` page renders two per-bot cards (squishy + otter) with a "Send echo" form via `<ServerForm>` so an operator can verify the round-trip end-to-end once the bot-side subscribers ship in Wave 7b. Sidebar Sudo-only group gains "RPC Test" (bot-owner only).
- **`user_profiles` editor — sudo full + self-service `/me/edit`.** `PATCH /api/squishy/profiles/[id]` is JSON-only, gated in-handler: sudo + bot owner get the full schema (incl. staff fields), self-service (id === viewing.id, no sudo) gets a restricted schema (names + birthday only). Upsert via Drizzle `ON CONFLICT (guild_id, user_id) DO UPDATE` so first-edit lazy-creates the row. Audits success + failure with `before`/`after`. New `/squishy/profiles/[id]/edit` page + `/me/edit` redirect. Detail page gains "Edit profile" button; sidebar gains "Edit my profile" link.
- **`business_owners` + `business_role_mappings` CRUD on `/otter/businesses/[slug]` (DB only; Discord side ships with Wave 7).** `POST/DELETE /owners` (bot-owner-only) + `POST/PATCH/DELETE /role-mappings` (bot-owner OR business-owner). Detail page gains `<OwnersCard>` + `<RoleMappingsCard>` with edit forms; both edit-enabled cards carry a yellow warning banner — "DB-only change. Discord role assignment / removal arrives with the Wave 7 command bus — this surface only writes the mapping table."

### Fixed
- **Dev clone preserves `/dev` prefix on internal `<Link>` navigation.** Clicking any link on `bots.tucker.host/dev/…` previously bounced you to the prod URL (no prefix) because Next.js didn't know it was being served from a subpath — Caddy was stripping `/dev` at the edge. Switched to Next.js's native `basePath`: `next.config.mjs` reads `NEXT_PUBLIC_BASE_PATH` at build time, the Dockerfile threads it through via `ARG`, and CI passes `=/dev` for the dev branch build (`github.ref_name == 'dev' && '/dev' || ''`) and empty for main. Caddy stops stripping the prefix since Next.js now serves `/dev/…` paths directly and renders all internal hrefs with the prefix automatically. Prod is unaffected.

### Added
- **`auto_join_roles` + `color_roles` CRUD on `/squishy/roles` (sudo-gated).** Builds on the Wave-6 write infra (CSRF + `writeAudit` + `<ServerForm>` + `withAuth` rate-limit). Four new JSON API routes under `web/src/app/api/squishy/`: `POST /auto-join-roles` (snowflake-validated, `ON CONFLICT(role_id) DO NOTHING` so re-adding is a `created:false` no-op rather than a 409) + `DELETE /auto-join-roles/[id]`; `POST /color-roles` (snowflake + label ≤100 + integer sortOrder; SELECT-then-INSERT-or-UPDATE so re-posting an existing role ID edits in place and only patches columns the caller actually sent — partial POSTs don't clobber existing sortOrder/label) + `PATCH /color-roles/[id]` + `DELETE /color-roles/[id]`. Every route is `withAuth({require:'sudo', csrf:true, rateLimit:{points:30, perSeconds:60}})` and audits both success and failure via `writeAudit({action:'auto_join_role.added|removed' or 'color_role.added|updated|removed', actor, viewing, before, after, success, errorMessage?})`. Schema gotchas the routes guard: `auto_join_roles.guildId` + `color_roles.guildId` are NOT NULL → routes refuse with `GUILD_ID-unset` when `env.GUILD_ID` isn't pinned; `color_roles.label` is NOT NULL → omitted labels on insert default to the role ID. UI: `/squishy/roles` gains sudo-only "Add role" / "Add color role" forms at the top of the auto-join and color tabs, per-row "Remove" buttons, and a collapsible inline "Edit label" form on each color card. All forms wrap `<ServerForm>` (`_format=json` opt-in, CSRF auto-injected) with `onSuccess={() => router.refresh()}` so the server-rendered table re-fetches without a full nav. Reaction-role tab stays read-only — creating a reaction-role message means posting in Discord, which needs the V2.5 command bus. Bot reads both tables live on every `guildMemberAdd` / `/color` invocation, so DB writes take effect immediately — no cache invalidation needed.
- **Wave 6 write-side infrastructure** — CSRF double-submit cookie, `writeAudit({bot, actor, viewing, action, before, after, ...})`, `<ServerForm>` client wrapper, in-memory per-actor rate limit. Foundation for every Wave-6+ edit UI.
- **`/otter/oc-stock` full CRUD for managers + bot owner.** Add item, edit status/URL inline, flip-to-confirm Remove. `POST` / `PATCH` / `DELETE` routes with zod validation, manager+ in-handler gate, read-before-write so audit captures `before`/`after`.
- **`bot_settings` inline edit (sudo) + `sudo_users` add/remove (bot owner).** `/squishy/settings` becomes editable per-row (`<input>` or auto-grow `<textarea>` + Save + Clear). `PUT /api/squishy/settings/[key]` upserts with `.onConflictDoUpdate`, `DELETE` clears, both audit `setting.changed` / `setting.cleared`. `/sudo` admin home gains an "Add sudo user" form + per-row Revoke button on DB rows only (env rows are immutable at runtime). `POST /api/sudo/users` audits `sudo.granted`, `DELETE /api/sudo/users/[id]` audits `sudo.revoked`. Both sudo write routes are bot-owner-only.
- **Otter notes + standings write endpoints — rank-gated per business.** New API routes wired on the Wave-6 write infra (`POST /api/otter/businesses/[slug]/standings`, `POST /api/otter/businesses/[slug]/notes`, `DELETE /api/otter/businesses/[slug]/notes/[noteId]`), all `withAuth({ csrf: true, rateLimit })` and funnelling success + every rejection through the shared `writeAudit({ bot: 'otter', ... })` so the audit log shows attempted escalations alongside actual writes. Standings upsert via Drizzle `onConflictDoUpdate` on `(business_id, character_id)` — one row per character per business, any rank (or bot owner) can record one, `before`/`after` captured for the audit diff. Notes enforce a visibility-vs-rank ceiling from `access.otter.businesses[slug]` (`employee → staff`, `manager → staff|manager`, `owner|botOwner → any`); over-rank `visibility` is rejected and audited `success=false`. Note delete is author OR business owner OR bot owner, with the deleted row snapshotted into the audit details. Standings + notes pages gain `<ServerForm>`-based "Record standing" / "Add note" forms (visibility select capped at the user's max writable tier) plus per-card Delete on notes for eligible viewers; both forms call `router.refresh()` on success.
- **`/squishy/archives` — read-only Archived Channels overview (sudo-gated).** Lists every row in `archived_channels` ordered by `archived_at desc` (cap 100), URL-driven `?q=` ILIKE on `original_name`, Discord deep-link when `GUILD_ID` is set. Sidebar Squishy group gains "Archives" link. Game Nights companion page **deferred** — the vendored schema doesn't ship a `game_nights` model yet.
- **UX shell polish — redesigned public landing + 404 + dashboard loading skeleton + error boundary.** Public `/` is now a multi-section landing: wordmark hero, live **Bot status** row reading `getHeartbeats()` server-side (one card per bot with 🟢/🔴 + version + uptime + last-beat; missing bots render 🔴 "no heartbeat yet"), a "What you can do here" capabilities grid, and an auth slot that flips between Sign-in CTA and "Welcome back" + Open dashboard. Footer shows `GIT_SHA.slice(0,7)` + `BUILD_TIME`. New `web/src/app/not-found.tsx` renders a friendly 404 card. New `web/src/app/(dashboard)/loading.tsx` is a shell-shaped Tailwind `animate-pulse` skeleton so route transitions don't flash blank. New `web/src/app/(dashboard)/error.tsx` is a `'use client'` boundary surfacing `error.message` + optional `error.digest` with Try-again + Back-to-home actions.

### Fixed
- **Removed duplicate `/audit/*` and `/squishy/voice/*` page files** that were left in `web/src/app/` after #58 moved them into the `(dashboard)/` route group. Next.js refused to build with "You cannot have two parallel pages that resolve to the same path." The rename was recorded in git but the old files persisted on disk and got reintroduced via rebase replays of Wave 3/4 branches that branched from a pre-rename base. Deleting the orphaned originals; the canonical pages live at `web/src/app/(dashboard)/audit/{page,AuditLive}.tsx` and `web/src/app/(dashboard)/squishy/voice/{page,VoiceLive}.tsx`.

### Docs
- **CLAUDE.md gains rule 6 — "Dev-first branching".** Documents the canonical workflow: feature branches target `dev`, verify on `bots.tucker.host/dev/`, then a promotion PR fast-forwards `main` to dev's HEAD. Hot fixes get a documented carve-out (`--base main` allowed when prod is broken + immediate main→dev sync PR required). Bot repos stay direct-to-main since they have no dev clone. Deployment section updated to reflect the two-tag / two-clone / one-watchtower layout.

### Fixed
- **OAuth callback now redirects to `PUBLIC_BASE_URL`, not the internal Next listen address.** Caddy reverse-proxies to `next-prod:3000`, so `req.url` inside the callback handler sees `http://0.0.0.0:3000` as its origin — and `new URL('/', url.origin)` was sending users to that internal address after a successful Discord auth, breaking sign-in end-to-end with "this webpage may have moved permanently". Replaced all four callback redirects (success + 3 error paths) with a `homeUrl(qs)` helper that derives the canonical URL from `env.PUBLIC_BASE_URL` (the same value already used to build the OAuth redirect_uri).
- **Per-clone DNS alias for the `next` service** so prod and dev Caddies don't round-robin between each other's Next.js containers. Both clones declared `next` as a service name on the shared `botpanel-net`, which made docker's DNS resolver alternate between them — so `bots.tucker.host/api/auth/login` was sometimes generating a `/dev` redirect_uri (and vice-versa) because the request reached the wrong Next.js process. New compose `networks.botpanel-net.aliases: [${NEXT_ALIAS:-next-prod}]` adds a unique alias per clone (`next-prod` for main, `next-dev` for dev), and the Caddyfile now reads `${NEXT_HOST:-next-prod}` so each clone's Caddy targets the right container.

### Changed
- **`cloudflared` moved out of the compose stack to a standalone `docker run` container.** Previously the cloudflared service was managed by compose, which meant `docker compose up -d` on the botpanel stack could (and did, today) recreate the container and wipe its `TUNNEL_TOKEN`. The new pattern runs cloudflared as a long-lived standalone container with `--network host` (so Cloudflare ingress rules pointing at `localhost:6080`/`6081` reach the prod/dev Caddy containers directly) and reads the token from `/home/botuser/cloudflared/.env` (chmod 600). One cloudflared fronts both clones, survives any compose teardown, and gets auto-updated by the shared watchtower via the `com.centurylinklabs.watchtower.enable=true` label. The `docker run` command lives in a comment block in `docker-compose.yml` for future setups. CHANGELOG / `.env.example` / `CLAUDE.md` updated accordingly; `CF_TUNNEL_TOKEN` removed from `.env.example` since the stack no longer reads it.

### Added
- **`/sudo/debug` — bot-owner-only Debug Snapshot.** Three cards via `Promise.allSettled`: feature flags from `bot_settings` filtered with `like(key, 'feature.%')` (enabled/disabled pills), live heartbeats from `getHeartbeats()`, last 20 `setting_changes`. Sidebar Sudo-only group gains "Debug" link. V2 (cache clear, orphan scan, flag toggles) lands with the Redis command bus.
- **Per-business drill-down pages — `/otter/businesses/[slug]/{standings,notes,audit}`.** Standings: full paginated table with `?character=` ILIKE filter. Notes: visibility-gated in the WHERE clause via `inArray(notes.visibility, allowed)` so a manager never sees an owner-only note body in the payload (employee → `[staff]`, manager → `[staff, manager]`, owner → all). Audit: full table with `?action=` filter + click-to-expand JSON details. The existing detail page gains "View all" links into each drill-down.
- **`/sudo` Admin Home — bot-owner-only cross-cutting roll-up** of sudo users (env + DB merged with `env`/`db` source pills), pending staff approvals (last 50), and recent reports (last 30 with status pills + GitHub issue links). Three sections via `Promise.allSettled` for per-section DB-down degradation. Gated on `access.botOwner`. Sidebar Sudo-only group gains "Admin Home" link.
- **`/squishy/automation` — auto-thread channels + social feeds tabbed page, sudo-gated.** Server-side tabs (`?tab=threads|feeds`), per-tab DB degradation, Discord deep-links when `GUILD_ID` is set. Sidebar gains "Automation" link.
- **`/squishy/roles` — auto-join + color + reaction-role messages in one tabbed page, sudo-gated.** Server-side tabs (`?tab=join|color|reaction`). Reaction sub-tab includes "Open message in Discord" deep-link via new `discordMessageUrl()` helper. Sidebar gains "Roles" link.
- **`/squishy/profiles` directory + `/squishy/profiles/[id]` detail (sudo-gated, read-only).** List view reads up to 200 `user_profiles` rows ordered by `updated_at desc` with URL-driven substring search via `?q=…` (ILIKE on `user_id` + `display_name`). Detail view loads the profile + `user_game_prefs` ⨯ `games` join + `staff_approvals` history in parallel and renders four sections: header card (deterministic HSL colored-initial circle keyed on `userId.slice(-3)`), profile fields grid (real/display name + formatted birthday with pings-off/year-hidden badges + staff category/department/tier/leadership-title), game preferences table, staff role requests history with status pills and click-to-expand `requested_data` JSON. Per-section DB-unavailable degradation. Sidebar gains "Profiles" link in the Squishy group.
- **Shared dashboard chrome — persistent sidebar nav + redesigned `/me` with a capabilities map.** New `web/src/app/(dashboard)/` route group bundles every authed page under one shell: `layout.tsx` (server) resolves the session + `resolveAccess()` once and hands the AccessMap to a `<DashboardShell>` server component which renders `<Sidebar>` (client, `'use client'`) plus a `<main className="md:pl-60">` content slot. Sidebar groups: Home + Dashboard always; Squishy (Settings / Hubs / Games / Active Voice) gated on `squishy.sudo || botOwner`; Otter (Businesses / OC Stock) gated on `Object.keys(otter.businesses).length > 0 || botOwner`; Sudo-only (Audit Log) on `sudo || botOwner`. Active link highlighting via `usePathname()` (`bg-bg-card2 text-ink` vs `text-ink-dim hover:bg-bg-card2/50`). Mobile (<md) collapses to a hamburger button + slide-in drawer with backdrop; desktop (≥md) shows a 240px fixed left sidebar. Bottom user card shows Discord avatar (CDN URL from `session.avatar`) + username + Sign out form posting to `/api/auth/logout`. Existing pages moved into the group via `git mv` — `web/src/app/(dashboard)/me/page.tsx`, `web/src/app/(dashboard)/squishy/voice/{page,VoiceLive}.tsx`, `web/src/app/(dashboard)/audit/{page,AuditLive}.tsx` — URLs unchanged because route groups in parens don't affect routing. The public `/` home stays at the root layout so unauthenticated visitors still see the sign-in card; signed-in viewers there now just see an "Open dashboard" CTA (the old inline `/squishy/voice` + `/audit` links were dropped — those live in the sidebar now). `/me` itself was rewritten end-to-end: avatar + username + Discord ID + bot-owner badge, a Capabilities card rendering `resolveAccess()` in human-readable form (Bot Owner yes/no, Squishy sudo yes/no, Squishy voice channels with click-through chips, Otter businesses with per-business rank pills colored owner/manager/employee), plus a `<details>` toggle that pretty-prints the raw AccessMap for debugging. Edge-middleware matcher already covered every protected path — no change needed there.

### Fixed
- **OAuth `prompt=none` → `prompt=consent` so first-time sign-in actually works.** With `prompt=none`, Discord silently refused the consent screen for users who hadn't previously authorized the app and bounced them back without a code — the callback then redirected to `/?error=missing_params` and the home page didn't show the error, so sign-in appeared to silently no-op. Forcing `prompt=consent` always shows the screen. Home page (`web/src/app/page.tsx`) now also surfaces `?error=...` in a red card so future OAuth failures are visible instead of silent.
- **OAuth callback now redirects to `PUBLIC_BASE_URL`, not the internal Next listen address.** Caddy reverse-proxies to `next-prod:3000`, so `req.url` inside the callback handler sees `http://0.0.0.0:3000` as its origin — and `new URL('/', url.origin)` was sending users to that internal address after a successful Discord auth, breaking sign-in end-to-end with "this webpage may have moved permanently". Replaced all four callback redirects (success + 3 error paths) with a `homeUrl(qs)` helper that derives the canonical URL from `env.PUBLIC_BASE_URL` (the same value already used to build the OAuth redirect_uri).
- **Per-clone DNS alias for the `next` service** so prod and dev Caddies don't round-robin between each other's Next.js containers. Both clones declared `next` as a service name on the shared `botpanel-net`, which made docker's DNS resolver alternate between them — so `bots.tucker.host/api/auth/login` was sometimes generating a `/dev` redirect_uri (and vice-versa) because the request reached the wrong Next.js process. New compose `networks.botpanel-net.aliases: [${NEXT_ALIAS:-next-prod}]` adds a unique alias per clone (`next-prod` for main, `next-dev` for dev), and the Caddyfile now reads `${NEXT_HOST:-next-prod}` so each clone's Caddy targets the right container.

### Added
- **Read-only `/squishy/hubs` and `/squishy/games` overview pages (sudo-gated).** Two new server-rendered pages under `web/src/app/(dashboard)/squishy/` give sudo viewers a configured-state view of the Squishy voice + game surface without needing a Discord client. `/squishy/hubs` renders every `hub_channels` row ordered by `position asc, created_at asc` with the per-hub defaults, lockdown window, and a **live count of currently-spawned `auto_channels`** for that hub (Drizzle `leftJoin` on `auto_channels.source_hub_id = hub_channels.channel_id` + `groupBy` so zero-spawn hubs still appear; the live-count cell deep-links to `/squishy/voice`). `/squishy/games` lists every `games` row ordered by `name asc` and zips in per-game `wants_view` / `wants_ping` counts from `user_game_prefs` via a single grouped query. Both pages render Discord deep-links (`https://discord.com/channels/<guildId>/<channelId>`) when `GUILD_ID` is configured, fall back to plain mono text otherwise, and show explicit empty / DB-unavailable states instead of 500-ing. Shared formatting helpers live in `web/src/lib/util/format.ts` (`relTime`, `formatDuration` — `null → 'default'`, `0 → 'disabled'`, seconds → `'30 min'` / `'2h 15m'` — and `discordChannelUrl`). Non-sudo viewers get a 403 card with a back-to-dashboard link.
- **Otter businesses dashboard — capability-gated index + per-business detail.** New server-rendered pages under the `(dashboard)` route group: `web/src/app/(dashboard)/otter/businesses/page.tsx` is a grid of cards (one per business the viewer can see; bot owner sees all including deactivated as faded cards, everyone else sees only the businesses in `access.otter.businesses`), with provider pill, owner-count + role-mapping-count aggregates (single `groupBy(businessId)` per metric — no N+1), and a per-card rank pill (Owner gold / Manager blue / Employee gray / Bot Owner purple). `web/src/app/(dashboard)/otter/businesses/[slug]/page.tsx` (Next 15 `params: Promise<{slug}>` shape) renders the business header, an Owners list, a Role-mappings table (sorted owner → manager → employee → name), the last 20 standings with color-coded pills, the last 20 audit rows with click-to-expand `<details>` showing the raw target + details JSON, and a notes-by-visibility count grid. Gate is `botOwner || access.otter.businesses[slug] != null` with friendly 403/404 cards instead of redirects so URLs stay linkable from staff embeds. Each Drizzle query is individually try/catch'd — per-card "data unavailable" instead of 500ing. Display helpers in `web/src/lib/util/otterFormat.ts`.
- **Read-only `/squishy/settings` viewer for the `bot_settings` table.** New server component at `web/src/app/(dashboard)/squishy/settings/page.tsx` gates on `resolveAccess()` (sudo or bot owner; non-authorized viewers see a "Not authorized" card so the URL stays linkable) and reads `bot_settings` directly via Drizzle (`squishyDb.select().from(botSettings).orderBy(asc(key))`) — no API round-trip from the same Node process. DB-down degrades to an empty list with an inline banner instead of a 500. Rows are passed to `SettingsView.tsx` (client) which groups by the first dot-segment of `key` ("feature", "voice", "staff", …), renders each namespace as a `<details open>` block with key/value/updated/by columns, truncates values longer than 80 chars (full value in a click-to-expand `<details>` + `title` tooltip), shows `updated_by_discord_id` as `<@id>` mention-style, and live-filters on a substring search against `key`. New `GET /api/squishy/settings` (`withAuth({ require: 'sudo' })`, Node runtime) returns the same `{ settings: [...] }` shape for scripts.
- **Public OC stock viewer at `/otter/oc-stock`.** New server-rendered page (lives in the `(dashboard)` route group so the shared sidebar wraps it) mirrors what Otterbot's `/oc` slash-command shows in Discord — intentionally NOT sudo-gated, because OC stock is already public in-server. Edge middleware bounces un-authed visits; the page renders for any logged-in user. Reads `oc_stock` via typed Drizzle (`asc(sortOrder), asc(name)`); per-item card shows `🟢/🟠/🔴` status emoji + matching ring color (`opacity-50` on out-of-stock), bold name (linked with an `ExternalLink` icon to `url` when set, `target="_blank" rel="noopener noreferrer"`), and a footer with hand-rolled relative-time + the updater's Discord ID. Edit capability detected via `resolveAccess()` — `canEdit = access.botOwner || ['owner','manager'].includes(access.otter.businesses['original-clothing'])` — surfaces a top-right "Manage stock" button rendered **disabled** with a V2 tooltip rather than linking to a 404. DB-unavailable degrades gracefully to an "OC stock not available right now" card; companion `GET /api/otter/oc-stock` (Node runtime, `withAuth({ require: 'any' })`) returns 200 with `{ items: [], error: 'db-unavailable' }` so future client-side polling sees a consistent shape.
- **Live `/squishy/voice/` page — Active Voice Channels in real time.** Server-rendered shell (`web/src/app/squishy/voice/page.tsx`) gates on `resolveAccess()` (sudo or bot owner) and hands off to a client component (`web/src/app/squishy/voice/VoiceLive.tsx`) that pulls an initial snapshot from `GET /api/squishy/voice/list` (Drizzle-typed read over `auto_channels` ⨯ `auto_channel_members`, ordered by `created_at desc`, returns `{channels:[], error:'db-unavailable'}` with 200 if Postgres is unreachable so the page still renders) and then merges deltas streamed by `GET /api/squishy/voice/stream`. The stream route runs on the Node runtime (`runtime = 'nodejs'`, ioredis needs `net`), psubscribes to `bot.squishy.voice.*` via the shared lazy-singleton subscriber in `lib/redis.ts`, frames each event as `data: <json>\n\n`, emits a `: ping\n\n` comment every 30s to stay under Cloudflare's 100s idle timeout, and detaches its `pmessage` listener on `req.signal.abort` so a long-running tab leaving never starves the process. Both API routes are wrapped in `withAuth({ require: 'sudo' })`. Client merges seven event types — `channel_created`, `channel_deleted`, `member_join`, `member_leave`, `lock_toggled`, `hidden_toggled`, `owner_changed` — keyed by `voiceChannelId` in a Map (O(1) point-updates) and renders a `🟢 Live` / `🔴 Reconnecting` indicator from `EventSource.readyState`. Username resolution is deferred to V2 — IDs are shown verbatim. Home page (`web/src/app/page.tsx`) gains a link for logged-in users; per-page gating still happens server-side.
- **Unified live audit tail at `/audit`.** New sudo-gated page (`web/src/app/audit/page.tsx` + `AuditLive.tsx`) renders the last 50 entries merged from Squishy `setting_changes` and Otter `audit_logs`, then live-tails new events over SSE. `GET /api/audit/list?limit=N&bot=all|squishy|otter` returns the initial snapshot normalized to a single `{ bot, id, ts, actor, action, summary, raw }` shape (capped at 200, per-source DB errors surfaced as `errors[]` so a downed DB degrades to a partial table instead of a 500). `GET /api/audit/stream` (Node runtime, `force-dynamic`) psubscribes to `bot.squishy.settings.setting_changed` and `bot.otter.audit.written` via the shared `lib/redis.ts` singleton and emits the same unified shape over SSE with a 30s keep-alive comment; the per-client handler is detached on disconnect but the singleton psubscription is left in place so concurrent viewers don't race. Client UI: bot toggle (`All | Squishy | Otter`), substring search over `summary|actor|action`, click-to-expand `raw` JSON, briefly-pulsing new rows, a `🟢 Live` / `🔴 Reconnecting` connection pill, and a hand-rolled `relTime()` helper so we don't pull a date-formatting dep just for relative timestamps. Otter-only staff don't see the page in MVP (gate is `botOwner || squishy.sudo`) — their portal pages will surface a per-business slice later. `/audit/*` added to the edge-middleware matcher so un-authed visits bounce to `/`. Landing page gets an "Audit tail (sudo)" link for signed-in users; the page itself shows a 403 card for non-sudo viewers rather than redirecting, so the URL stays linkable.
- **`/api/healthz` now aggregates live bot heartbeats from Redis pub/sub.** A lazy-singleton ioredis subscriber (`web/src/lib/redis.ts`) psubscribes to `bot.*.bot.heartbeat` and a module-level Map in `web/src/lib/heartbeats.ts` tracks the latest `{ version, uptime, ts, guildCount? }` per bot. The healthz response now includes a `bots` map keyed by bot name with `lastSeen` (ISO), `uptime`, `version`, optional `guildCount`, plus `online: true` and `lastBeatSec` for the landing-page renderer. Entries older than 3× the 60s publish tick (180s) are omitted so the landing page falls back to "no heartbeat yet" instead of showing stale uptime. Redis being unreachable is non-fatal — the Map stays empty and the route still returns 200 with `bots: {}`.
- **Vendored Drizzle schemas + sync script.** Bot schemas remain the source of truth in `squishybot/` and `otterbot/`; botpanel keeps read-only copies under `web/src/lib/db/schema/{squishy,otter}/`, regenerated by `scripts/sync-schema.sh` (rsync from each bot's `src/db/schema/`, prepends an `AUTO-GENERATED — DO NOT EDIT` banner to every `.ts` file, idempotent on re-run). Override the source repos via `SQUISHY_REPO` / `OTTER_REPO` env vars (defaults to `/home/botuser/projects/{squishybot,otterbot}` for local runs). Botpanel still **never** runs `drizzle-kit generate` or `db:migrate` — migrations remain owned by the bot repos.
- **Drizzle clients `squishyDb` / `otterDb`.** New `web/src/lib/db/{squishy,otter}.ts` using `drizzle-orm/postgres-js` + the `postgres` 3.x driver. Clients are lazy via a Proxy — importing the module never opens a connection, and any access without `SQUISHY_DATABASE_URL` / `OTTER_DATABASE_URL` set throws a clear actionable error rather than crashing the dashboard at boot.
- **CI `verify-schemas` job.** New job runs before the build matrix, checks out `jason-tucker/squishybot` + `jason-tucker/otterbot` into `/tmp`, runs `pnpm verify:schemas` in `web/` (re-runs the sync script and fails on `git diff --exit-code` against `src/lib/db/schema`). Build jobs `needs: verify-schemas` so schema drift short-circuits the pipeline before image builds.
- **`web/package.json` scripts:** `sync:schemas` (run the script) and `verify:schemas` (run + diff-check). `drizzle-orm` and `postgres` were already in `dependencies`; running `pnpm install --lockfile-only` produced the initial `pnpm-lock.yaml`.

### Changed
- **`cloudflared` moved out of the compose stack to a standalone `docker run` container.** Previously the cloudflared service was managed by compose, which meant `docker compose up -d` on the botpanel stack could (and did, today) recreate the container and wipe its `TUNNEL_TOKEN`. The new pattern runs cloudflared as a long-lived standalone container with `--network host` (so Cloudflare ingress rules pointing at `localhost:6080`/`6081` reach the prod/dev Caddy containers directly) and reads the token from `/home/botuser/cloudflared/.env` (chmod 600). One cloudflared fronts both clones, survives any compose teardown, and gets auto-updated by the shared watchtower via the `com.centurylinklabs.watchtower.enable=true` label. The `docker run` command lives in a comment block in `docker-compose.yml` for future setups. CHANGELOG / `.env.example` / `CLAUDE.md` updated accordingly; `CF_TUNNEL_TOKEN` removed from `.env.example` since the stack no longer reads it.

### Added
- **Capabilities-map `resolveAccess()` + `withAuth` middleware.** `web/src/lib/auth/perms.ts` exports a flat `AccessMap` (actor + viewing identity, `botOwner`, `squishy.{sudo,voiceChannels,canSelfEdit}`, `otter.businesses`) resolved from env (`BOT_OWNER_ID`, new optional `SUDO_USER_IDS`) plus the squishy `sudo_users` / `auto_channels` and otter `business_role_mappings` / `business_owners` tables. DB calls are individually try/catch'd so a downed Postgres degrades to empty capabilities rather than 500-ing the panel. View-As is plumbed in (sudo/owner-only; `actor` always reflects the real user, `viewing` swaps to the target; non-sudo callers silently fall back to self). `web/src/lib/auth/middleware.ts` adds `withAuth(handler, { require: 'any'|'sudo'|'botOwner' })` that 401s without a session, 403s on capability gate failure, and forwards the resolved `AccessMap` to the handler. `web/src/middleware.ts` is a thin Edge cookie-presence redirect for `/squishy/*`, `/otter/*`, `/sudo/*`, `/me` (no DB calls in middleware). `GET /api/me` returns the AccessMap for client-side nav rendering. Includes stub `web/src/lib/db/{squishy,otter}.ts` + `web/src/lib/db/schema/{squishy,otter}/index.ts` placeholders so this PR builds before the parallel schema-sync PR lands; `scripts/sync-schema.sh` overwrites them on merge.
- **MVP foundation — Next.js 15 dashboard scaffolding.** New `web/` subdirectory with the App Router app (`web/src/app/`), Tailwind + dark theme palette matching the landing page, zod-validated env (`web/src/lib/env.ts`), Discord OAuth helper (`web/src/lib/auth/discord.ts`), JWT session via `jose` in a `__Host-` cookie with 3-day sliding TTL (`web/src/lib/auth/session.ts`). Routes shipped: `GET /` (sign-in / signed-in card), `GET /me` (auth-gated demo page that redirects to login), `GET /api/auth/login` (random-state Discord OAuth redirect), `GET /api/auth/callback` (state-verified token exchange + session mint), `POST /api/auth/logout`, `GET /api/auth/me`, `GET /api/healthz` (returns 200 — landing page status badge will go 🟢 once this image is up). Auth disabled until `DISCORD_CLIENT_ID` + `DISCORD_CLIENT_SECRET` + `SESSION_SECRET` + `PUBLIC_BASE_URL` are set in `.env`; without them the home page shows the sign-in button but `/api/auth/login` returns 503. Multi-stage Dockerfile (`web/Dockerfile`) outputs Next standalone on `node:22-alpine`.
- **docker-compose.yml gains a `next` service** alongside the existing caddy + cloudflared + watchtower. `NEXT_IMAGE` env var picks the floating tag (`:latest` / `:dev`). No host port mapping; Caddy proxies via the docker network. Watchtower auto-pulls when CI pushes a new image.
- **docker-compose.yml gains a `redis` service** (`redis:7-alpine`, internal only — `expose: 6379`, no host port). Joins `botpanel-net` so both bot stacks reach it as `redis:6379` over the shared external network. `--save 60 1` keeps a small AOF-free snapshot in a `redis_data` volume; `redis-cli ping` healthcheck. This is the single bus for events (bots → panel) AND commands (panel → bots, V2+) per the locked architecture.
- **Caddyfile flipped to `reverse_proxy next:3000`** with `handle_errors` falling back to `landing/502.html` on any 5xx so the page is always something useful. The `uri strip_prefix /dev` line from the path-based routing is preserved.
- **CI builds two images per push** — `botpanel:<tag>` (Caddy ingress, with bundled landing) and `botpanel-web:<tag>` (Next.js dashboard). Separate GHA cache scopes so a Next.js change doesn't bust the Caddy cache.

### Added
- **Phase 0 — landing page + Caddy ingress + Cloudflare Tunnel scaffolding.** Single Caddy container behind a `cloudflared` tunnel on the shared `botpanel-net` external docker network. Listens internally on `${PORT:-6080}` — NO host port mapping anywhere. Caddyfile serves `landing/index.html` (status indicator + helpful messaging for offline/updating/online states); `502.html` is the fallback Caddy serves when the future Next.js dashboard returns 5xx (also auto-refreshes every 10s). Build args bake the git SHA and build time into a meta tag the page reads.
- **Static landing page.** `landing/` is plain HTML/CSS/JS — no framework. Polls `/api/healthz` every 10s with a 3s timeout. Renders `online` / `updating` / `offline` based on response, plus per-bot heartbeat rows fed by the payload. Designed as "what you can still do right now" rather than a coming-soon splash.

### Security
- **All ingress flows through Cloudflare Tunnel.** No ports are bound on the VPS host — `cloudflared` connects outbound to Cloudflare's edge. The only world-bound port on the host stays SSH (`:22`).
- **Shared `botpanel-net` external docker network** so botpanel can reach the bots' Postgres DBs without any host port mapping. Both `squishybot` and `otterbot` compose stacks were updated in the same change to remove their public Postgres bindings and join this network with stable aliases `db-squishy` / `db-otter`.

_v0.2.0 · 3aaf806_
